<script lang="ts">
	// import type { LayoutData } from './$types';

	// export let data: LayoutData;

	// const session = writable();
	// $: session.set(data.session);

	// setContext('session', { getSession: () => session });
</script>

<slot />
